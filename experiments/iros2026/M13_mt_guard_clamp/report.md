# M13 — exit-guard throttle clamp

Same M08 line as M12, plus `u ≤ v` while outward. First stall **t = 21.50, s = 18.47 (hp1 apex, INSIDE e_lat +0.37)**. Along-track **+0.44 m at turn-in**. Guard never applies to an inside cut. After recoveries, timed laps **9.32–9.62**. Rejected: slower and still colliding.
