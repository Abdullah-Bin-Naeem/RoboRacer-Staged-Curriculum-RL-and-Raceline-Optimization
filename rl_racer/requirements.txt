# .venv-rl -- exact versions this branch was verified with (Python 3.10, Ubuntu 22.04).
# torch is the CUDA 12.9 build; a machine with an older NVIDIA driver (< 575)
# needs the cu126 index instead: --index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://download.pytorch.org/whl/cu129
torch==2.13.0+cu129
stable_baselines3==2.9.0
gymnasium==1.3.0
numpy==1.26.4          # 1.x: the venv shares rclpy with ROS Humble's system python
tensorboard==2.21.0
cloudpickle==1.6.0
